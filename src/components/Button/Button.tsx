import './Button.scss';

const Button = () => {
    return (
        <div className="container">
            <div className="container-color">
                Button
            </div>
        </div>
    );
};

export default Button;